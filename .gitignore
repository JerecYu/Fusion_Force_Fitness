# 道具倉庫 — 靠 package.json 就能重新叫貨，不需要上傳
node_modules/

# 裝箱區產出 — 由原始碼產生，不需要進版本控制
dist/
build/

# 金鑰與環境變數 — 絕對不能上傳
.env
.env.local
*.key

# 作業系統與編輯器產生的雜物
.DS_Store
Thumbs.db
desktop.ini
.vscode/

# ══════════════════════════════════════════════════════════════
# 客人個資：絕對不能進 Git（這個 repo 是公開的）
#
# 完整手機號碼在 HANDOVER.md 第十五節屬於「櫃檯保險箱，永不上雲」。
# 這幾條是保險絲 —— 就算哪天手滑 git add -A，也不會被推上去。
# ══════════════════════════════════════════════════════════════
*.xlsx
*.xls
*.csv
客人*
customer-list*
!tools/customer-list-check.html
migration-local/

# ☢️ 2026-08-19 圖文選單工具的產出 —— 裡面是 200 多位客人的 LINE 使用者編號
#    和顯示名稱。那是個資，絕對不能進版本控制。
tools/line-followers.csv
tools/line-staff-ids.txt

# ══════════════════════════════════════════════════════════════
# ☢️ 2026-08-19 本機資料夾 —— 絕對不能進版本控制
#
# 這個 repo 是【公開】的。《財務與教練薪資整合規則》裡有抽成級距、
# 鐘點費表、以及具名的固定加給（誰每月加多少）—— 那是薪資資料。
# 規則文件放這裡，程式讀得到、GitHub 看不到。
# ══════════════════════════════════════════════════════════════
local/
