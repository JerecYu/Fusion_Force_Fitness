// ═══════════════════════════════════════════════════════════════════
// line-hook — 【已停用】
//
// 專案：FFF 預約系統（fff-platform）· 第 67 步 · 2026-08-19
//
// 這一支的用途只有一天：收六位教練在 Messaging API 那個 Provider
// 的使用者編號，以便把【教練專屬圖文選單】綁到他們帳號上。
// 2026-08-19 上午收齊六位、綁定完成，所以它就退役了。
//
// ☢️ 為什麼要特地把它停掉，而不是「反正沒人呼叫就算了」：
//    它是一個 verify_jwt = false 的公開端點 —— 任何人都 POST 得到。
//    即使它只收「開頭是 STAFF」的訊息、危害很小，
//    【沒有用途的入口就該關掉】—— 這是原則問題，不是風險評估問題。
//    一個「不知道還開著」的入口，六個月後沒人記得它在做什麼。
//
// ☢️ LINE 那邊的 Webhook 開關也已經關掉（OA Manager → 回應設定）。
//    兩邊都斷 —— 只斷一邊的話，下次有人把 LINE 那個開關打開，
//    這支程式就又默默地開始收資料了。
//
// ☢️ 資料表 line_messaging_ids【保留】—— 以後新增教練、
//    或要重新綁定選單時還用得到那六個編號。
//
// 要重新啟用：把這一支換回原本的版本（git 歷史裡有），
// 再到 OA Manager 把 Webhook 打開。兩件事都做才會生效。
//
// ── 原本的版本做什麼（給以後的自己看）──────────────────────────
//   收 LINE 的 webhook 事件 → 只挑 message/text 而且【開頭是 STAFF】的
//   → upsert 進 line_messaging_ids（line_user_id, note）
//   → 一律回 200（LINE 收不到 200 會重送，連續失敗還會自動關掉 webhook）
//   沒有驗 X-Line-Signature：那需要把 Channel secret 放進環境變數，
//   而這張表只存「編號 ＋ 一段自己打的字」，我們是拿它跟六個講好的
//   暗號對照，不是照單全收 —— 偽造的列進得來，但配不上那六句話。
// ═══════════════════════════════════════════════════════════════════

Deno.serve(() => {
  // 什麼都不存。依然回 200 —— 萬一 LINE 還在呼叫，
  // 回錯誤只會讓它一直重送。
  return new Response('disabled')
})
